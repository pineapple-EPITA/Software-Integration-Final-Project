import '@jest/globals';

// Do not redeclare global namespace - Jest will handle this
// The conflict occurs because these are already declared elsewhere

// Export the Jest globals for convenience, but don't redeclare them
export {}; 